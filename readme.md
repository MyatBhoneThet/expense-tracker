<!-- backend -->
<!-- URL -> Router -> Middleware -> Controller -> JSON Response -->
<!-- config: Purpose: Connects your app to MongoDB using mongoose.connect(...).
Why? Centralizes database setup, making it easy to manage DB connection logic. -->

<!-- controllers: Holds all business logic. Controllers receive requests from routes, process them, and respond. -->

<!-- middleware: Middlewares are functions that run between the request and the final controller. -->

<!-- models: Mongoose models define the structure of your MongoDB documents. -->

<!-- routes: Holds route definitions. These route files connect to controller functions. -->

<!-- uploads: Contains uploaded files, like user profile pictures. -->

<!-- frontend -->
<!-- assets: storing photos -->

<!-- components: Reusable UI pieces (buttons, inputs, layouts).
Inputs: A styled input field (used in forms).
layouts: Holds layout components, which wrap pages to provide a consistent structure (like headers, footers, auth styling).-->

<!-- pages: Main screens of your app.
Auth: authentication pages
Dashboard: pages for app-->

<!-- utils: it holds helper files that provide utility functions or constants — things you want to reuse across your app but that aren’t tied to a specific component or page. -->
